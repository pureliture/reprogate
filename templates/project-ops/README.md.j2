# AI Ops Local Runtime

This directory is reserved for generated AI Ops runtime artifacts such as process context.

Do not commit volatile session data here unless the repository explicitly treats it as a durable adapter file.
