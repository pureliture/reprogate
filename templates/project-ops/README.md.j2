# ReproGate Local Records

This directory is reserved for generated ReproGate workspace artifacts such as Work Records and Gates.

Do not commit volatile session data here unless the repository explicitly treats it as a durable artifact file.
