# Workspace Profile: {{ project_name }}

## Identity

- Workspace: `{{ primary_workspace_name }}`
- Branch: `{{ primary_branch }}`
- Runtime: `{{ primary_runtime_value }}`

## Notes

- Update this file when the primary branch strategy or runtime expectations change.
- If the repository uses multiple workspaces, add the secondary workspace rules below.
