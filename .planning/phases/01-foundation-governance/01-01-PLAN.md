---
phase: 01-foundation-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reprogate.yaml
  - templates/reprogate.yaml.j2
  - scripts/cli.py
  - scripts/create_record.py
  - scripts/init.py
  - pyproject.toml
  - scripts/tests/test_record_creation.py
  - scripts/tests/test_cli.py
autonomous: true
requirements: [CORE-01, CORE-04, CORE-05]

must_haves:
  truths:
    - "User can run `reprogate create --type adr --title X` and get a valid ADR file"
    - "User can run `reprogate create --type rfc --title X` and get a valid RFC file"
    - "Created records use sequential IDs matching existing convention (ADR-008, RFC-004)"
    - "User sees ReproGate branding in CLI help, not dpc"
    - "User can run `reprogate check` and it invokes gatekeeper.py"
    - "reprogate.yaml includes active_skills list and record_types config"
    - "init.py generates reprogate.yaml with the expanded schema"
    - "pyproject.toml registers a reprogate console-script entry point"
  artifacts:
    - path: "reprogate.yaml"
      provides: "Canonical config with active_skills, record_types, gatekeeper section"
      contains: "active_skills"
    - path: "scripts/create_record.py"
      provides: "Record creation for ADR and RFC types"
      exports: ["create_record", "main"]
    - path: "scripts/cli.py"
      provides: "Unified CLI entry point with reprogate branding"
    - path: "pyproject.toml"
      provides: "Console-script entry point for reprogate command"
      contains: "reprogate"
  key_links:
    - from: "scripts/cli.py"
      to: "scripts/create_record.py"
      via: "subprocess.run dispatch for create command"
    - from: "scripts/cli.py"
      to: "scripts/gatekeeper.py"
      via: "subprocess.run dispatch for check command"
    - from: "scripts/init.py"
      to: "templates/reprogate.yaml.j2"
      via: "render_template reads template and writes reprogate.yaml"
    - from: "pyproject.toml"
      to: "scripts/cli.py"
      via: "console_scripts entry point"
---

<objective>
Lock the canonical config schema, build record creation, and establish the unified CLI entry point.

Purpose: Both reviewers identified config schema inconsistency as a cross-cutting issue -- `init.py` and `generate.py` disagree on structure. Codex flagged that CORE-05 requires a real `reprogate` console-script entry point, not just updating `scripts/cli.py`. Record IDs must follow the existing `ADR-NNN` convention (not date-based). This plan locks the config contract first, then builds CLI and record creation on top.

Output: Expanded `reprogate.yaml` schema, updated `init.py` and template, working `scripts/create_record.py`, rebranded `scripts/cli.py`, console-script in `pyproject.toml`, and tests for CLI and record creation.
</objective>

<execution_context>
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-governance/01-CONTEXT.md
@.planning/phases/01-foundation-governance/01-RESEARCH.md

<interfaces>
<!-- Current CLI structure the executor must understand -->

From scripts/cli.py (current):
```python
SCRIPT_DIR = pathlib.Path(__file__).resolve().parent

def run_script(script_name: str, args: List[str]) -> int:
    proc = subprocess.run([sys.executable, str(SCRIPT_DIR / script_name), *args], check=False)
    return proc.returncode

def parse_args(argv: List[str] | None = None) -> argparse.Namespace:
    parser = argparse.ArgumentParser(description="dpc helper CLI.")  # <-- must rename
    parser.add_argument("command", choices=["init", "generate", "check", "search", "search-content", "print"])
    # "check" routes to check_compliance.py which does NOT exist on disk
```

From scripts/init.py (current):
```python
ROOT = pathlib.Path(__file__).resolve().parents[1]
TEMPLATE_PATH = ROOT / "templates" / "reprogate.yaml.j2"
OUTPUT_PATH = ROOT / "reprogate.yaml"
def render_template(text: str, context: Dict[str, str]) -> str  # {{ key }} replacement
def build_context(args: argparse.Namespace) -> Dict[str, str]  # builds template vars
def main(argv: List[str] | None = None) -> int
```

From scripts/generate.py load_config (current -- expects nested structure):
```python
def load_config(path: pathlib.Path) -> Dict[str, Any]:
    default_data: Dict[str, Any] = {
        "project": {},
        "workspaces": {"primary": {}},
        "processes": {"enabled": []},
        "tools": {"claude": {}, "codex": {}},
        "records": {},
    }
```

Current reprogate.yaml (flat structure):
```yaml
project_name: "ReproGate"
reprogate_version: "1.0.0"
skills_dir: "skills"
records_dir: "records"
gatekeeper:
  engine: "opa"
  strict_mode: true
```

Existing record naming convention:
- ADR-001, ADR-002, ..., ADR-007 (sequential, zero-padded 3 digits)
- RFC-001, RFC-002, RFC-003 (sequential, zero-padded 3 digits)
- Filenames: ADR-001-config-schema.md, RFC-001-reprogate-prototype.md

Work record template (templates/records/WORK-RECORD-TEMPLATE.md):
  Frontmatter: record_id="WR-YYYYMMDD-001", title, status="DRAFT", created_at
  Sections: Intent, Scope, Decisions, Verification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock canonical config schema and update init.py</name>
  <files>reprogate.yaml, templates/reprogate.yaml.j2, scripts/init.py</files>
  <read_first>
    - reprogate.yaml
    - templates/reprogate.yaml.j2
    - scripts/init.py
    - scripts/generate.py (lines 31-60 -- understand load_config expectations)
    - docs/governance/constitution.md
  </read_first>
  <action>
    1. Update `reprogate.yaml` to the canonical expanded schema. This is the single source of truth all scripts must agree on:
       ```yaml
       # ReproGate Configuration
       project_name: "ReproGate"
       reprogate_version: "1.0.0"

       # Record storage
       records_dir: "records"
       record_types:
         adr:
           dir: "adr"
           prefix: "ADR"
           required_sections: ["Context", "Decision", "Consequences", "Verification"]
         rfc:
           dir: "rfc"
           prefix: "RFC"
           required_sections: ["Summary", "Design / Proposal", "Verification"]

       # Skill definitions
       skills_dir: "skills"
       active_skills:
         - record-required
         - decision-documented
         - scope-defined
         - verification-present

       # Gatekeeper behavior
       gatekeeper:
         engine: "opa"
         strict_mode: true
         fail_closed: true
       ```
       Key differences from old schema: adds `record_types` with per-type config, adds `active_skills` list, adds `fail_closed`, keeps `engine: "opa"` per ADR-002.

    2. Update `templates/reprogate.yaml.j2` to match:
       ```yaml
       # ReproGate Configuration
       project_name: "{{ project_name }}"
       reprogate_version: "1.0.0"

       records_dir: "records"
       record_types:
         adr:
           dir: "adr"
           prefix: "ADR"
           required_sections: ["Context", "Decision", "Consequences", "Verification"]
         rfc:
           dir: "rfc"
           prefix: "RFC"
           required_sections: ["Summary", "Design / Proposal", "Verification"]

       skills_dir: "skills"
       active_skills:
         - record-required
         - decision-documented
         - scope-defined
         - verification-present

       gatekeeper:
         engine: "opa"
         strict_mode: true
         fail_closed: true
       ```

    3. Update `scripts/init.py`:
       - The current init.py has many args for a complex template (workspaces, processes, claude, codex). The new template is simpler. Remove args that no longer apply (`--primary-workspace-name`, `--primary-branch`, `--primary-runtime`, `--enabled-processes`, `--disable-claude`, `--disable-claude-hook-enforcement`, `--disable-codex`, `--wp-path`, `--adr-path`, `--changelog-path`).
       - Keep: `--output`, `--project-name`, `--force`.
       - Update `build_context()` to return only `{"project_name": args.project_name}`.
       - Keep `render_template()` function unchanged (it handles `{{ key }}` replacement).
       - Remove `DEFAULT_PROCESSES` constant.

    Per D-02: Config is harness-agnostic. No GSD or adapter terminology.
    Per D-09: Uses ReproGate-native naming only.

    IMPORTANT: `generate.py` currently expects a very different nested config structure (`project.name`, `workspaces.primary`, `tools.claude`, etc.). Do NOT modify generate.py in this plan -- it will need separate reconciliation work. The canonical schema defined here is for the ReproGate governance core (gatekeeper, skills, records). The generate.py adapter-generation config is a separate concern that can coexist or be reconciled later.
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && python3 -c "import yaml; d=yaml.safe_load(open('reprogate.yaml')); assert 'active_skills' in d, 'missing active_skills'; assert 'record_types' in d, 'missing record_types'; assert d['gatekeeper']['fail_closed'] == True, 'missing fail_closed'; assert d['record_types']['adr']['prefix'] == 'ADR'; print('Config schema valid')"</automated>
  </verify>
  <acceptance_criteria>
    - reprogate.yaml contains the key `active_skills:` followed by a YAML list of 4 skill names
    - reprogate.yaml contains `record_types:` with nested `adr:` and `rfc:` blocks each having `prefix`, `dir`, and `required_sections`
    - reprogate.yaml contains `gatekeeper:` with `engine: "opa"`, `strict_mode: true`, `fail_closed: true`
    - templates/reprogate.yaml.j2 contains `{{ project_name }}` placeholder
    - templates/reprogate.yaml.j2 contains `active_skills:` section
    - scripts/init.py does NOT contain `DEFAULT_PROCESSES`
    - scripts/init.py `build_context` returns dict with key `project_name`
    - `python3 -c "import yaml; yaml.safe_load(open('reprogate.yaml'))"` exits 0
  </acceptance_criteria>
  <done>reprogate.yaml has canonical expanded schema. Template matches. init.py generates it correctly with only project_name as variable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create record scaffolding, CLI rebranding, and console-script entry point</name>
  <files>scripts/create_record.py, scripts/cli.py, pyproject.toml, scripts/tests/test_record_creation.py, scripts/tests/test_cli.py</files>
  <read_first>
    - scripts/cli.py (current -- has dpc branding and broken check routing)
    - templates/records/WORK-RECORD-TEMPLATE.md (template for work records)
    - scripts/generate.py (reference for coding style: shebang, argparse, type hints, ROOT constant)
    - pyproject.toml (current -- needs console-script and pytest dev dep)
    - records/adr/ADR-007-uv-toolchain-adoption.md (latest ADR -- to verify next sequential ID would be ADR-008)
    - records/rfc/RFC-003-product-boundary.md (latest RFC -- next would be RFC-004)
    - reprogate.yaml (Task 1 output -- read record_types config for prefix and required_sections)
  </read_first>
  <behavior>
    - Test: create_record("adr", "Test Decision") produces file records/adr/ADR-008-test-decision.md (next sequential ID)
    - Test: created ADR has YAML frontmatter with record_id="ADR-008", type="adr", status="DRAFT", title, created_at
    - Test: created ADR has sections from config: Context, Decision, Consequences, Verification
    - Test: create_record("rfc", "API Design") produces file records/rfc/RFC-004-api-design.md
    - Test: created RFC has sections: Summary, Design / Proposal, Verification
    - Test: sequential creation increments correctly (ADR-008, ADR-009)
    - Test: invalid type raises ValueError
    - Test: CLI help contains "ReproGate" and does NOT contain "dpc"
    - Test: CLI "check" routes to gatekeeper.py
    - Test: CLI "create" routes to create_record.py
  </behavior>
  <action>
    1. Add pytest dev dependency and console-script to `pyproject.toml`:
       ```toml
       [project.optional-dependencies]
       dev = ["pytest>=7.0"]

       [project.scripts]
       reprogate = "scripts.cli:main"
       ```
       Then run `uv add --dev pytest` to sync lockfile.

       NOTE on console-script: The `scripts.cli:main` entry point requires `scripts/` to be importable as a package. If `scripts/__init__.py` does not exist, create an empty one. Alternatively, if the subprocess-based `cli.py` pattern is preferred (it calls scripts via subprocess, not imports), use a simpler wrapper:
       ```toml
       [project.scripts]
       reprogate = "scripts.cli:main"
       ```
       AND add `scripts/__init__.py` (empty file) so the package is importable.
       If this approach proves problematic (cli.py uses `subprocess.run` internally, which works regardless of import), it is acceptable. The key requirement is `reprogate` appears as an installable command.

    2. Create `scripts/tests/test_record_creation.py` with pytest tests:
       - Use `tmp_path` fixture for all tests
       - `test_create_adr_file_exists`: call `create_record("adr", "Test", output_dir=tmp_path)`, assert file exists matching `ADR-*-test.md` pattern in `tmp_path/adr/`
       - `test_create_adr_frontmatter`: verify created file has YAML frontmatter with `record_id`, `type: "adr"`, `status: "DRAFT"`, `title`, `created_at`
       - `test_create_adr_sections`: verify file contains `# Context`, `# Decision`, `# Consequences`, `# Verification`
       - `test_create_rfc_file_exists`: similar for RFC type
       - `test_create_rfc_sections`: verify `# Summary`, `# Design / Proposal`, `# Verification`
       - `test_sequential_id`: create two ADRs in same tmp_path, verify IDs are NNN and NNN+1
       - `test_invalid_type`: `pytest.raises(ValueError)` for type="invalid"
       - `test_slug_generation`: title "My Test Decision" produces slug "my-test-decision" in filename

    3. Create `scripts/create_record.py`:
       - Shebang: `#!/usr/bin/env python3`
       - `ROOT = pathlib.Path(__file__).resolve().parents[1]`
       - `RECORDS_DIR = ROOT / "records"`
       - Load `reprogate.yaml` via `yaml.safe_load` to get `record_types` config. Fallback to hardcoded defaults if config missing.
       - Function `slugify(title: str) -> str`: lowercase, replace non-alphanumeric with hyphens, strip leading/trailing hyphens, collapse consecutive hyphens.
       - Function `next_id(record_dir: pathlib.Path, prefix: str) -> str`:
         - Scan existing files matching `{prefix}-*.md` in record_dir
         - Extract numeric part (e.g., "007" from "ADR-007-foo.md")
         - Return `{prefix}-{max+1:03d}` (e.g., "ADR-008")
         - If no files exist, return `{prefix}-001`
       - Function `create_record(record_type: str, title: str, output_dir: pathlib.Path | None = None) -> pathlib.Path`:
         - Validate record_type against keys in record_types config (default: "adr", "rfc"). Raise ValueError if invalid.
         - Get type config: `prefix`, `dir`, `required_sections`
         - Target dir: `(output_dir or RECORDS_DIR) / type_config["dir"]`
         - Create dir if not exists
         - Generate record_id via `next_id(target_dir, prefix)`
         - Generate slug via `slugify(title)`
         - Filename: `{record_id}-{slug}.md` (e.g., `ADR-008-test-decision.md`)
         - Write frontmatter:
           ```
           ---
           record_id: "{record_id}"
           title: "{title}"
           type: "{record_type}"
           status: "DRAFT"
           created_at: "{today ISO}"
           ---
           ```
         - Write sections from `required_sections` as `# {section_name}` with placeholder text
         - Return file path
       - `parse_args()`: `--type` (required, choices from record_types keys), `--title` (required), `--output-dir` (optional)
       - `main()`: parse args, call create_record, print "Created {path}", return 0. Error handling: ValueError -> stderr, return 1.

    4. Create `scripts/tests/test_cli.py` with pytest tests:
       - `test_help_shows_reprogate`: capture `cli.py --help` output via subprocess, assert "ReproGate" in output
       - `test_help_no_dpc`: assert "dpc" NOT in help output
       - `test_create_command_accepted`: `parse_args(["create"])` does not raise
       - `test_check_routes_to_gatekeeper`: grep cli.py source for `gatekeeper.py` in check routing
       - `test_all_commands_accepted`: verify each command in ["init", "generate", "check", "create", "search", "search-content", "print"] is accepted by parse_args

    5. Update `scripts/cli.py`:
       - Change description: `"ReproGate CLI - repository governance tooling."`
       - Add `"create"` to choices: `["init", "generate", "check", "create", "search", "search-content", "print"]`
       - Fix check routing: `run_script("gatekeeper.py", extra)` instead of `run_script("check_compliance.py", extra)`
       - Add create routing: `if args.command == "create": return run_script("create_record.py", extra)`
       - Add `"gate"` as alias for `"check"` (per D-09 ReproGate-native vocabulary)

    Per D-09: CLI uses "ReproGate" branding, harness-neutral. No GSD vocabulary.
    Per D-10: No workflow-specific command vocabulary.
    Addresses review concern: Record IDs follow existing ADR-NNN convention (Codex MEDIUM concern).
    Addresses review concern: pyproject.toml console-script for CORE-05 (Codex HIGH concern).
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && uv add --dev pytest 2>/dev/null; uv run python3 -m pytest scripts/tests/test_record_creation.py scripts/tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/create_record.py contains `def create_record(record_type: str, title: str`
    - scripts/create_record.py contains `def next_id(` with logic scanning existing files
    - scripts/create_record.py contains `def slugify(`
    - scripts/create_record.py does NOT contain `YYYYMMDD` in ID generation (uses sequential NNN)
    - scripts/cli.py contains `"ReproGate CLI"` in description
    - scripts/cli.py does NOT contain the string `"dpc"`
    - scripts/cli.py contains `"create"` in choices
    - scripts/cli.py contains `gatekeeper.py` for check routing
    - pyproject.toml contains `[project.scripts]` with `reprogate`
    - pyproject.toml contains `pytest` in optional-dependencies or dev dependencies
    - scripts/tests/test_record_creation.py contains at least 6 test functions
    - scripts/tests/test_cli.py contains at least 4 test functions
    - `uv run python3 -m pytest scripts/tests/test_record_creation.py scripts/tests/test_cli.py -x` exits 0
  </acceptance_criteria>
  <done>Record creation uses sequential ADR-NNN IDs matching existing convention. CLI shows ReproGate branding, routes correctly, and pyproject.toml registers the reprogate console-script. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Config schema valid
cd /Users/pureliture/IdeaProjects/reprogate && python3 -c "import yaml; d=yaml.safe_load(open('reprogate.yaml')); assert 'active_skills' in d and 'record_types' in d; print('OK')"

# All plan 01 tests pass
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/test_record_creation.py scripts/tests/test_cli.py -v

# CLI help shows reprogate branding
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 scripts/cli.py --help | grep -i reprogate

# CLI create works end-to-end (in temp dir to avoid repo pollution)
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 scripts/create_record.py --type adr --title "Test" --output-dir /tmp/reprogate-test && cat /tmp/reprogate-test/adr/ADR-001-test.md
```
</verification>

<success_criteria>
- reprogate.yaml has canonical expanded schema with active_skills, record_types, and fail_closed
- init.py generates the expanded config correctly
- `reprogate create --type adr --title "X"` produces ADR with sequential ID and correct frontmatter/sections
- `reprogate create --type rfc --title "X"` produces RFC with sequential ID and correct frontmatter/sections
- `reprogate check` invokes gatekeeper.py
- CLI help says "ReproGate", not "dpc"
- pyproject.toml has console-script entry
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance/01-01-SUMMARY.md`
</output>
