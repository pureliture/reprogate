---
phase: 01-foundation-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cli.py
  - scripts/create_record.py
  - scripts/tests/test_cli.py
  - scripts/tests/test_record_creation.py
autonomous: true
requirements: [CORE-01, CORE-05]
must_haves:
  truths:
    - "User can run reprogate create --type adr --title X and get a valid ADR file"
    - "User can run reprogate create --type rfc --title X and get a valid RFC file"
    - "User sees reprogate branding in CLI help, not dpc"
    - "User can run reprogate check and it invokes gatekeeper.py"
    - "All existing CLI commands (init, generate, search, search-content, print) still work"
  artifacts:
    - path: "scripts/create_record.py"
      provides: "Record creation logic for ADR and RFC types"
    - path: "scripts/cli.py"
      provides: "Unified CLI entry point with reprogate branding"
    - path: "scripts/tests/test_cli.py"
      provides: "CLI routing and branding tests"
    - path: "scripts/tests/test_record_creation.py"
      provides: "Record creation unit tests"
  key_links:
    - from: "scripts/cli.py"
      to: "scripts/create_record.py"
      via: "subprocess.run dispatch for create command"
    - from: "scripts/cli.py"
      to: "scripts/gatekeeper.py"
      via: "subprocess.run dispatch for check command"
---

<objective>
Establish the unified CLI entry point and record creation capability.

Purpose: Users need a single `reprogate` CLI to create work records (ADRs/RFCs) and run all framework commands. The current CLI has legacy "dpc" branding, routes `check` to a missing file, and lacks a `create` command.

Output: Working `scripts/cli.py` with reprogate branding, `create` subcommand, and fixed `check` routing. New `scripts/create_record.py` for record scaffolding. Tests for both.
</objective>

<execution_context>
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-governance/01-CONTEXT.md
@.planning/phases/01-foundation-governance/01-RESEARCH.md

<interfaces>
<!-- Current CLI structure the executor must understand -->

From scripts/cli.py (current):
```python
SCRIPT_DIR = pathlib.Path(__file__).resolve().parent

def run_script(script_name: str, args: List[str]) -> int:
    proc = subprocess.run([sys.executable, str(SCRIPT_DIR / script_name), *args], check=False)
    return proc.returncode

def parse_args(argv: List[str] | None = None) -> argparse.Namespace:
    parser = argparse.ArgumentParser(description="dpc helper CLI.")  # <-- needs rename
    parser.add_argument("command", choices=["init", "generate", "check", "search", "search-content", "print"])
    # check routes to check_compliance.py which does NOT exist
```

Work record template frontmatter (from templates/records/WORK-RECORD-TEMPLATE.md):
  Fields: record_id="WR-YYYYMMDD-001", title="...", status="DRAFT", created_at="YYYY-MM-DD"
  Sections: Intent, Scope, Decisions, Verification

Existing record types in records/:
- records/adr/*.md (type: "adr", frontmatter has record_id, type, status)
- records/rfc/*.md (type: "rfc", frontmatter has record_id, type, status)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create record scaffolding script and tests</name>
  <files>scripts/create_record.py, scripts/tests/test_record_creation.py, pyproject.toml</files>
  <read_first>
    - scripts/create_record.py (will not exist yet -- confirm absence)
    - templates/records/WORK-RECORD-TEMPLATE.md (template to scaffold from)
    - scripts/generate.py (reference for coding style: shebang, argparse, type hints, ROOT constant pattern)
    - scripts/init.py (reference for argparse pattern and file creation flow)
    - pyproject.toml (to add pytest dev dependency)
    - records/adr/ADR-001-config-schema.md (example of existing ADR frontmatter to match)
  </read_first>
  <behavior>
    - Test: create_record("adr", "Config Schema") produces a file at records/adr/ADR-YYYYMMDD-001.md
    - Test: created ADR file has YAML frontmatter with record_id, type="adr", status="DRAFT", title, created_at
    - Test: created ADR file has sections: Context, Decision, Consequences, Verification
    - Test: create_record("rfc", "API Design") produces a file at records/rfc/RFC-YYYYMMDD-001.md
    - Test: created RFC file has YAML frontmatter with record_id, type="rfc", status="DRAFT", title, created_at
    - Test: created RFC file has sections: Summary, Design / Proposal, Verification
    - Test: sequential creation in same day increments the counter (001, 002, 003)
    - Test: invalid type raises error (not adr or rfc)
    - Test: main() with --type adr --title "Test" returns exit code 0
  </behavior>
  <action>
    1. Add pytest to dev dependencies in pyproject.toml:
       ```
       [project.optional-dependencies]
       dev = ["pytest>=7.0"]
       ```
       Then run `uv add --dev pytest` to install and update uv.lock.

    2. Create `scripts/tests/test_record_creation.py` with the behavior tests above. Use pytest style (not unittest). Each test creates a temp directory, calls the function, asserts file exists and content matches. Use `tmp_path` fixture.

    3. Create `scripts/create_record.py` following existing script patterns:
       - Shebang: `#!/usr/bin/env python3`
       - Constants: `ROOT = pathlib.Path(__file__).resolve().parents[1]`
       - `RECORDS_DIR = ROOT / "records"`
       - `TEMPLATES_DIR = ROOT / "templates" / "records"`
       - Function `create_record(record_type: str, title: str, output_dir: pathlib.Path | None = None) -> pathlib.Path`:
         - Validate record_type in ("adr", "rfc"), raise ValueError otherwise
         - Generate record_id: for adr use `ADR-YYYYMMDD-NNN`, for rfc use `RFC-YYYYMMDD-NNN`
         - NNN = count existing files in target dir matching today's date prefix + 1
         - Target dir: `output_dir / record_type` or `RECORDS_DIR / record_type`
         - Create target dir if not exists
         - Render frontmatter with these exact fields:
           record_id: the generated ID (e.g. "ADR-20260325-001")
           type: the record_type value ("adr" or "rfc")
           title: the user-provided title
           status: "DRAFT"
           created_at: today's date in ISO format (YYYY-MM-DD)
         - For adr, add sections: `# Context`, `# Decision`, `# Consequences`, `# Verification`
         - For rfc, add sections: `# Summary`, `# Design / Proposal`, `# Verification`
         - Write file, return path
       - `parse_args()` with argparse: `--type` (required, choices=["adr", "rfc"]), `--title` (required), `--output-dir` (optional)
       - `main()` calls create_record, prints "Created {relative_path}", returns 0
       - Error handling: try/except ValueError, print to stderr, return 1

    Per D-09: Use ReproGate-native terminology only (record, ADR, RFC). No GSD vocabulary.
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/test_record_creation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/create_record.py contains `def create_record(record_type: str, title: str`
    - scripts/create_record.py contains `def main(`
    - scripts/create_record.py contains `"adr", "rfc"` as valid choices
    - scripts/create_record.py contains `record_id` and `status` and `DRAFT` in the template rendering
    - scripts/tests/test_record_creation.py contains at least 6 test functions (test_create_adr, test_create_rfc, test_adr_frontmatter, test_rfc_sections, test_sequential_counter, test_invalid_type)
    - pyproject.toml contains `pytest` in dependencies
    - `uv run python3 -m pytest scripts/tests/test_record_creation.py -x` exits 0
  </acceptance_criteria>
  <done>All record creation tests pass. ADR and RFC records can be created programmatically with correct frontmatter and sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CLI branding, routing, and add create command</name>
  <files>scripts/cli.py, scripts/tests/test_cli.py</files>
  <read_first>
    - scripts/cli.py (current state -- has "dpc" branding and check_compliance.py routing)
    - scripts/create_record.py (created in Task 1 -- verify it exists and has main())
    - scripts/gatekeeper.py (the correct target for "check" command -- verify main() interface)
  </read_first>
  <behavior>
    - Test: CLI help text contains "reprogate" and does NOT contain "dpc"
    - Test: CLI accepts "create" as a valid command
    - Test: CLI accepts all existing commands: init, generate, check, search, search-content, print
    - Test: "check" command routes to gatekeeper.py (not check_compliance.py)
    - Test: "create" command routes to create_record.py
    - Test: unknown command returns non-zero exit code
    - Test: parse_args(["create"]) returns command="create"
  </behavior>
  <action>
    1. Create `scripts/tests/test_cli.py` with pytest-style tests for all behaviors above. For routing tests, use subprocess to call `cli.py create --type adr --title Test` in a temp dir and verify it succeeds. For help text, capture `cli.py --help` output and assert "reprogate" present and "dpc" absent.

    2. Update `scripts/cli.py`:
       - Change description from `"dpc helper CLI."` to `"ReproGate CLI - repository governance tooling."`
       - Add `"create"` to choices list: `choices=["init", "generate", "check", "create", "search", "search-content", "print"]`
       - Change check routing from `run_script("check_compliance.py", extra)` to `run_script("gatekeeper.py", extra)`
       - Add create routing: `if args.command == "create": return run_script("create_record.py", extra)`
       - Keep all other routing unchanged

    Per D-09: CLI description uses "ReproGate" branding, harness-neutral.
    Per D-10: No GSD vocabulary in CLI surface.
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/cli.py contains `"ReproGate CLI"` in the description string
    - scripts/cli.py does NOT contain the string `"dpc"`
    - scripts/cli.py contains `"create"` in the choices list
    - scripts/cli.py contains `gatekeeper.py` for the check command routing (not check_compliance.py)
    - scripts/cli.py contains `create_record.py` for the create command routing
    - scripts/tests/test_cli.py contains at least 4 test functions
    - `uv run python3 -m pytest scripts/tests/test_cli.py -x` exits 0
  </acceptance_criteria>
  <done>CLI shows reprogate branding, routes check to gatekeeper.py, routes create to create_record.py, all tests pass.</done>
</task>

</tasks>

<verification>
```bash
# All plan 01 tests pass
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/test_record_creation.py scripts/tests/test_cli.py -v

# CLI help shows reprogate branding
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 scripts/cli.py --help | grep -i reprogate

# CLI create command works end-to-end
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 scripts/cli.py create -- --type adr --title "Test ADR" && ls records/adr/ADR-*.md
```
</verification>

<success_criteria>
- `reprogate create --type adr --title "X"` produces a valid ADR file with correct frontmatter and sections
- `reprogate create --type rfc --title "X"` produces a valid RFC file with correct frontmatter and sections
- `reprogate check` invokes gatekeeper.py successfully
- CLI help text says "ReproGate", not "dpc"
- All tests in test_record_creation.py and test_cli.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance/01-01-SUMMARY.md`
</output>
