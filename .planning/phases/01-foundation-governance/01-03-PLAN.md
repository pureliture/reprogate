---
phase: 01-foundation-governance
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - reprogate.yaml
  - scripts/init.py
  - scripts/tests/test_bootstrap_smoke.py
  - scripts/tests/test_integration.py
autonomous: true
requirements: [CORE-04]

must_haves:
  truths:
    - "reprogate.yaml schema includes skills activation list and gate strictness config"
    - "init.py generates a reprogate.yaml with the expanded schema"
    - "Full flow works: init -> create record -> gatekeeper check passes"
    - "Existing bootstrap smoke tests still pass (or are updated to match new state)"
  artifacts:
    - path: "reprogate.yaml"
      provides: "Expanded config schema with skills activation and record type config"
      contains: "active_skills"
    - path: "scripts/init.py"
      provides: "Updated init script generating expanded reprogate.yaml"
      exports: ["main"]
    - path: "scripts/tests/test_integration.py"
      provides: "End-to-end integration tests for full init->create->check flow"
    - path: "scripts/tests/test_bootstrap_smoke.py"
      provides: "Updated bootstrap tests compatible with current codebase state"
  key_links:
    - from: "scripts/init.py"
      to: "reprogate.yaml"
      via: "generates config file from template"
      pattern: "reprogate\\.yaml"
    - from: "scripts/gatekeeper.py"
      to: "reprogate.yaml"
      via: "reads active_skills and strict_mode from config"
      pattern: "active_skills|strict_mode"
---

<objective>
Expand the reprogate.yaml schema, update init.py, and add integration tests for the full flow.

Purpose: The current reprogate.yaml is minimal (only project_name, skills_dir, records_dir, gatekeeper section). It needs to express which skills are active, record type definitions, and gate behavior. init.py needs to generate this expanded config. Integration tests verify the full init->create->check pipeline works end-to-end.

Output: Expanded reprogate.yaml schema, updated init.py template, fixed bootstrap tests, and new integration tests.
</objective>

<execution_context>
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pureliture/IdeaProjects/reprogate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-governance/01-CONTEXT.md
@.planning/phases/01-foundation-governance/01-RESEARCH.md
@.planning/phases/01-foundation-governance/01-01-SUMMARY.md
@.planning/phases/01-foundation-governance/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From scripts/create_record.py (created in Plan 01):
```python
def create_record(record_type: str, title: str, output_dir: pathlib.Path | None = None) -> pathlib.Path
def main(argv: List[str] | None = None) -> int
```

<!-- From Plan 02 outputs -->
From scripts/gatekeeper.py (refactored in Plan 02):
```python
VERSION = "0.3.0"
def load_config() -> Dict[str, Any]:  # uses yaml.safe_load
def evaluate_gate(strict: bool = False) -> Tuple[int, List[str]]
```

From scripts/skill_evaluator.py (created in Plan 02):
```python
def evaluate_skill(skill_dir: pathlib.Path, input_data: Dict) -> RuleResult
def build_input_data(records: List[Tuple]) -> Dict
```

<!-- Current init.py structure -->
From scripts/init.py:
```python
ROOT = pathlib.Path(__file__).resolve().parents[1]
TEMPLATE_PATH = ROOT / "templates" / "reprogate.yaml.j2"
OUTPUT_PATH = ROOT / "reprogate.yaml"
def render_template(text: str, context: Dict[str, str]) -> str
def main(argv: List[str] | None = None) -> int
```

<!-- Current reprogate.yaml -->
```yaml
project_name: "ReproGate"
reprogate_version: "1.0.0"
skills_dir: "skills"
records_dir: "records"
gatekeeper:
  engine: "opa"
  strict_mode: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand reprogate.yaml schema and update init.py</name>
  <files>reprogate.yaml, templates/reprogate.yaml.j2, scripts/init.py</files>
  <read_first>
    - reprogate.yaml (current minimal schema)
    - templates/reprogate.yaml.j2 (template used by init.py to generate reprogate.yaml)
    - scripts/init.py (current init script -- understand parse_args, render_template, build_context)
    - scripts/gatekeeper.py (Plan 02 version -- verify it reads config keys we add)
    - docs/governance/constitution.md (governance rules that inform config design)
  </read_first>
  <action>
    1. Update `reprogate.yaml` to the expanded schema:
       ```yaml
       # ReproGate Configuration
       project_name: "ReproGate"
       reprogate_version: "1.0.0"

       # Record storage
       records_dir: "records"
       record_types:
         adr:
           dir: "adr"
           required_sections: ["Context", "Decision", "Consequences", "Verification"]
         rfc:
           dir: "rfc"
           required_sections: ["Summary", "Design / Proposal", "Verification"]

       # Skill definitions
       skills_dir: "skills"
       active_skills:
         - record-required
         - decision-documented
         - scope-defined
         - verification-present

       # Gatekeeper behavior
       gatekeeper:
         engine: "python-native"
         strict_mode: true
         fail_closed: true
       ```

    2. Update `templates/reprogate.yaml.j2` to match the expanded schema. Use `{{ project_name }}` placeholder for project_name. Use `{{ skills_list }}` for the active_skills block. Keep other values as sensible defaults.

    3. Update `scripts/init.py`:
       - Add `--strict-mode` flag (default true) to parse_args
       - Update `build_context()` to include:
         - `skills_list`: newline-joined list of default skills (`"  - record-required\n  - decision-documented\n  - scope-defined\n  - verification-present"`)
         - `strict_mode`: "true" or "false" from the flag
         - `engine`: "python-native" (hardcoded default, per research recommendation)
       - Keep all existing flags and behavior working

    Per D-02: Config is harness-agnostic. No GSD terminology in the schema.
    Per D-05: Gate authority from Skills -- active_skills list controls which skills the gatekeeper evaluates.
    Per D-09: Config uses ReproGate-native naming.
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && python3 -c "import yaml; data=yaml.safe_load(open('reprogate.yaml')); assert 'active_skills' in data; assert 'record_types' in data; assert data['gatekeeper']['strict_mode'] == True; print('Schema valid')"</automated>
  </verify>
  <acceptance_criteria>
    - reprogate.yaml contains `active_skills:` with a list of 4 skill names
    - reprogate.yaml contains `record_types:` with `adr:` and `rfc:` sub-keys
    - reprogate.yaml contains `gatekeeper:` with `engine: "python-native"` and `strict_mode: true`
    - reprogate.yaml contains `fail_closed: true`
    - templates/reprogate.yaml.j2 contains `{{ project_name }}` placeholder
    - templates/reprogate.yaml.j2 contains `active_skills` section
    - scripts/init.py contains `--strict-mode` in parse_args
    - `python3 -c "import yaml; yaml.safe_load(open('reprogate.yaml'))"` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>reprogate.yaml has expanded schema with active_skills, record_types, and fail_closed config. init.py generates it correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix bootstrap tests and add integration tests</name>
  <files>scripts/tests/test_bootstrap_smoke.py, scripts/tests/test_integration.py</files>
  <read_first>
    - scripts/tests/test_bootstrap_smoke.py (current tests -- references check_compliance.py which no longer exists)
    - scripts/cli.py (Plan 01 version -- verify create command works, check routes to gatekeeper.py)
    - scripts/create_record.py (Plan 01 version -- verify main() interface)
    - scripts/gatekeeper.py (Plan 02 version -- verify it runs standalone)
    - scripts/init.py (Task 1 version -- verify updated parse_args)
  </read_first>
  <behavior>
    - Test (bootstrap fix): test_bootstrap references gatekeeper.py instead of check_compliance.py
    - Test (bootstrap fix): setUpClass checks for gatekeeper.py existence, not check_compliance.py
    - Test (integration): init.py creates valid reprogate.yaml with expanded schema
    - Test (integration): create_record.py creates valid ADR with correct frontmatter
    - Test (integration): gatekeeper.py passes when valid records exist
    - Test (integration): gatekeeper.py fails when no records exist (fail-closed)
    - Test (integration): full pipeline: init -> create adr -> gatekeeper check passes
  </behavior>
  <action>
    1. Fix `scripts/tests/test_bootstrap_smoke.py`:
       - Replace all references to `check_compliance.py` with `gatekeeper.py`
       - Update `cls.check_script = cls.scripts_dir / "gatekeeper.py"`
       - Update test_check_compliance_passes_for_valid_structure to use gatekeeper flags
       - Update test_full_bootstrap_flow Step 3 to call gatekeeper.py instead of check_compliance.py
       - Fix the `result` variable reference bug in test_full_bootstrap_flow line 212 (uses undefined `result`, should be `check_result`)
       - Update assertions to match gatekeeper output format (Gate PASSED/FAILED)

    2. Create `scripts/tests/test_integration.py` with pytest tests:
       - Use `tmp_path` fixture for isolation
       - `test_init_creates_expanded_config`: Run init.py with subprocess into tmp_path, verify reprogate.yaml has active_skills and record_types keys via yaml.safe_load
       - `test_create_adr_produces_valid_record`: Run create_record.py with `--type adr --title "Test" --output-dir {tmp_path}`, verify file exists with correct frontmatter
       - `test_gatekeeper_passes_with_valid_records`: Set up tmp_path with records/adr/ containing a valid ADR, skills/ dir with at least record-required skill, reprogate.yaml pointing to them, run gatekeeper.py, assert exit code 0
       - `test_gatekeeper_fails_with_no_records`: Set up tmp_path with empty records/, skills with record-required, reprogate.yaml, run gatekeeper.py, assert exit code 1
       - `test_full_pipeline`: In tmp_path: run init.py to create config, copy skills/ from real repo, run create_record.py to create ADR, run gatekeeper.py, assert exit code 0 or expected output

    All tests use subprocess.run to call scripts as external processes (matching existing test patterns in test_bootstrap_smoke.py).
  </action>
  <verify>
    <automated>cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/test_integration.py scripts/tests/test_bootstrap_smoke.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - scripts/tests/test_bootstrap_smoke.py does NOT contain `check_compliance.py`
    - scripts/tests/test_bootstrap_smoke.py contains `gatekeeper.py`
    - scripts/tests/test_integration.py contains at least 4 test functions (test_init, test_create_adr, test_gatekeeper_passes, test_gatekeeper_fails or test_full_pipeline)
    - `uv run python3 -m pytest scripts/tests/test_integration.py -x` exits 0
    - `uv run python3 -m pytest scripts/tests/test_bootstrap_smoke.py -x` exits 0 (or tests are skipped with clear reason if check_compliance.py removal breaks them)
  </acceptance_criteria>
  <done>Bootstrap tests updated for current codebase. Integration tests verify the full init->create->check pipeline works end-to-end.</done>
</task>

</tasks>

<verification>
```bash
# All phase 1 tests pass together
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 -m pytest scripts/tests/ -x -v

# Full manual flow works
cd /Users/pureliture/IdeaProjects/reprogate && uv run python3 scripts/cli.py create -- --type adr --title "Test Phase 1" && uv run python3 scripts/gatekeeper.py
```
</verification>

<success_criteria>
- reprogate.yaml has expanded schema (active_skills, record_types, fail_closed)
- init.py generates configs with the expanded schema
- Bootstrap smoke tests pass with gatekeeper.py routing
- Integration tests verify the complete init->create->check pipeline
- All tests in scripts/tests/ pass: `uv run python3 -m pytest scripts/tests/ -v`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-governance/01-03-SUMMARY.md`
</output>
